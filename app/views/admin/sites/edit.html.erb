<h1>Editing Site</h1>

<%= form_with(model: @site, local: true) do |form| %>
  <%= render 'form', form: form, site: @site %>
<% end %>

<%= link_to 'Show', @site %> |
<%= link_to 'Back', sites_path %>
